"""Placeholder: the toy repo has no schema. Present so the profile's command resolves."""
import sys

if __name__ == "__main__":
    print(f"no schema in the toy repo; would have generated {sys.argv[1:]}")
