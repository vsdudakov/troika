# reviewer › Rules check 6: tests assert real behaviour, not that something ran.
role: reviewer
skill: internal-review
verdict: [Request changes]
expect_finding:
  severity: Blocker
  file: tests/service/test_allocation.py
  keywords_any: [hollow, real behaviour, real behavior, is not none, isinstance, asserts nothing, meaningless]
why: >
  Three tests that check only non-None and isinstance. They would pass against a
  function returning an empty dict for every input.
