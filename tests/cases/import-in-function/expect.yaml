# reviewer › Rules check 2 calls this out by name: "An import inside a function or
# method is a Major, not a nit."
role: reviewer
skill: internal-review
verdict: [Request changes]
expect_finding:
  severity: Major
  file: app/service/allocation.py
  keywords_any: [import, circular, top of the file, module level, deferred]
why: >
  The repository import is deferred into percent_by_symbol. Everything else matches
  the clean case, so a miss here is the gate failing, not the diff being ambiguous.
